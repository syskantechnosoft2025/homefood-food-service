apiVersion: apps/v1
kind: Deployment
metadata:
  name: food-service
  namespace: homefood
  labels:
    app: food-service
    version: "1.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: food-service
  template:
    metadata:
      labels:
        app: food-service
    spec:
      containers:
        - name: food-service
          image: ghcr.io/syskantechnosoft2025/homefood-food-service:latest
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: food-service
  namespace: homefood
spec:
  selector:
    app: food-service
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP